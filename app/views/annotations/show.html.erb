<header class="header_primary reverse_order">
  <div class="container">
    <nav class="header_nav">
      <%= link_to '⬅︎ Read full document', @current_annotation.contextual_path %>
    </nav>
    <h1><%= link_to 'DocumentCloud', '/' %></h1>
  </div>
</header>

<div class="container">
  <div id="DC-note-<%= @current_annotation.id %>" class="DC-note-container" style="background:white"></div>
</div>

<%
  @javascript_footer_inline = <<-EOS
    dc.embed.loadNote("#{@current_annotation.canonical_url(:js)}");
  EOS
%>
