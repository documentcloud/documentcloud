<script type="text/javascript">
window.RAILS_ENV = '<%= RAILS_ENV %>';
<% if Account.current %>
  dc.app.accountId = <%= Account.current.id %>;
  Accounts.add(new dc.model.Account(<%= Account.current.to_json %>));
<% end %>
</script>
