<%
  @title  = 'Open Source'
  @nav    = 'opensource'
  @header = 'All our work is open source software.'
%>

<div id="content">

  <p>
    We're
    <a href="http://www.pbs.org/idealab/2009/10/documentcloud-going-open-source-every-step-of-the-way302.html">releasing as we go</a>,
    so please follow our work if you're interested in what lies under the hood of
    DocumentCloud.
  </p>

  <h2>DocumentCloud Projects</h2>
  <ul class="bullet">
    <% @github.each do |name, url, github_url, title| %>
    <li>
      <h6>
        <a href="<%= h(url) %>"><%= h(name) %></a>
        <small><a href="<%= github_url %>" class="github_link"></a></small>
      </h6>
      <%= h(title) %>
    </li>
    <% end %>
  </ul>

  <h2>Our Code in the News</h2>
  <%= render :partial => "news" %>

</div>