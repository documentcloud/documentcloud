<%= @inviter_account.full_name %> at <%= @organization_name %> has invited you to review <% if @documents.count > 1 %><%= @documents.count %> documents.<% else %>"<%= @documents[0] && @documents[0].title %>."<% end %> Use the URL<% if @documents.count != 1 %>s<% end %> below to access the document<% if @documents.count != 1 %>s<% end %>.
<%- @documents.each do |d| -%>
<%- if @documents.count > 1 -%><%= d.title %><%- end %>
<%= DC.server_root(:ssl => true) %>/documents/<%= d.id %>-<%= d.slug %>.html<%= @key %>

<%- end -%>
<%- if @message && @message.length >= 1 -%>
<%= @inviter_account.first_name %> provided the following instructions:


<%= @message %>


<%- end -%>
Please do not share this URL, as it was created specifically for you. If you have colleagues who you think should also take a closer look, you can reach <%= @inviter_account.full_name %> at <%= @inviter_account.email %>.

<%= @organization_name %> is using DocumentCloud to share and annotate source documents. You can learn more at https://www.documentcloud.org.

The DocumentCloud Team
