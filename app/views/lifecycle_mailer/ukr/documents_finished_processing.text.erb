<%= @account.full_name %> --
<% form = (@count%10==1 && @count%100!=11 ? 0 : @count%10>=2 && @count%10<=4 && (@count%100<10 || @count%100>=20) ? 1 : 2) %>

<% if 0 == form %>
<%= @count %> документ було успішно опрацьовано і теперь його можна відкрити та відредагувати на DocumentCloud.

<% elsif 1 == form %>

<%= @count %> документа було успішно опрацьовано і теперь їх можна відкрити та відредагувати на DocumentCloud.

<% else %>

<%= @count %> документів було успішно опрацьовано і теперь їх можна відкрити та відредагувати на DocumentCloud.

<% end %>

<%= DC.server_root %>

PS. Якщо Вам потрібна допомога щодо використання DocumentCloud, ви можете почитати документацію (https://www.documentcloud.org/help) або написати на support@documentcloud.org.
