dc.ui.AdminAccounts = dc.View.extend({

  id        : 'admin_accounts',

  callbacks : {
  },

  constructor : function(options) {
    this.base(options);
  },

  render : function() {
    return this;
  }

});